[project]
name = "mrxsierra-github-io"
version = "0.1.0"
description = "my site"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material>=9.6.9",
    "mkdocs-blogging-plugin",
    "mkdocs-git-revision-date-localized-plugin",
]
